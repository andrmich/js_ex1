/**
 * Zamień przypisania zmiennych z formatu ES5 na ES6.
 */

const PI = 3.14;

let loopCounter = 0;

const VERSION = "0.12.2";

let checkNumber = function(number) {
  return number % 2 === 0;
};

let myCustomString = "Super string!";

let loopVariable = 0;

let stringAppender = function(string) {
  return string + " world!";
};
